export type RGB = {
    r: number, 
    g: number,
    b: number,
    a?: number,
}

export type HSV = {
    h: number,
    s: number,
    v: number,
    a?: number,
}
export type HSL = {
    h: number,
    s: number,
    l: number,
    a?: number,
}

export type XYZ = {
    x: number,
    y: number,
    z: number,
    a?: number,
}

export type LAB = {
    l: number,
    A: number,
    B: number,
    a?: number,
}

export type LCH = {
    l: number,
    c: number,
    h: number,
    a?: number,
}

