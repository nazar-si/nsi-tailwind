export type RGB = {
    r: number, 
    g: number,
    b: number,
}

export type HSV = {
    h: number,
    s: number,
    v: number,
}
export type HSL = {
    h: number,
    s: number,
    l: number,
}

export type XYZ = {
    x: number,
    y: number,
    z: number,
}

export type LAB = {
    l: number,
    a: number,
    b: number,
}

export type LCH = {
    l: number,
    c: number,
    h: number,
}

